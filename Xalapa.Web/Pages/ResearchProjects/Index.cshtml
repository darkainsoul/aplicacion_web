@page
@model Xalapa.Web.Pages.ResearchProjectsModel
@{
    ViewData["Title"] = "Proyectos de Investigacion";
}

<h2>Proyectos de Investigacion</h2>
<p>
    <a asp-page="Create">Crear Nuevo</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProyectoInvestigaciones.FirstOrDefault().id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProyectoInvestigaciones.FirstOrDefault().name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProyectoInvestigaciones.FirstOrDefault().date_register)
            </th>
            <th colspan=3>
                Comandos
            </th>

        </tr>
    </thead>
    <tbody>
        @if(Model.ProyectoInvestigaciones.Count() > 0)
        {
              @foreach (var item in Model.ProyectoInvestigaciones)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.date_register)
                    </td>
                    <td>
                        <a class="btn btn-warning" asp-page="./Edit" asp-route-id="@item.id">Editar</a> |
                        <a class="btn btn-info"  asp-page="./Details" asp-route-id="@item.id">Detalle</a> |
                        <a class="btn btn-danger"  asp-page="./Delete" asp-route-id="@item.id">Borrar</a>
                    </td>
                </tr>
            }
        }        
    </tbody>
</table>
